Extract only administrative eligibility conditions from the supplied source text. Preserve exact source_file_id, source_version, page, and verbatim_text evidence. Numeric conditions must preserve their explicit unit; use `원` only for values already expressed in won and `명` only for employee counts already expressed as people. Do not silently scale or convert a different unit. Return one Canonical IR object and do not infer unsupported facts.
