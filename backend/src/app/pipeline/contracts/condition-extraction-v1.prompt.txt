Extract only administrative eligibility conditions from the supplied source text. Preserve exact source_file_id, source_version, page, and verbatim_text evidence. The source_page_map lists paginated sources and their one-based page numbers; their source text contains matching page elements, and evidence from those sources must use that page number rather than null. Non-paginated sources use page null. Emit only compatible comparison shapes: enum subjects use ENUM with EQ/NE or STRING_SET with IN/NOT_IN; FOUNDED_ON uses DATE comparisons; ELIGIBLE_REGION uses REGION_SET with IN/NOT_IN; ANNUAL_REVENUE and EMPLOYEE_COUNT use INTEGER comparisons or RANGE with BETWEEN; text subjects use STRING for equality, containment, or SEMANTIC_MATCH and STRING_SET for IN/NOT_IN. EXISTS has a null expected_value. Numeric conditions must preserve their explicit unit; use `원` only for values already expressed in won and `명` only for employee counts already expressed as people. Every other condition has a null unit. Do not silently scale or convert a different unit. Return one Canonical IR object and do not infer unsupported facts.
