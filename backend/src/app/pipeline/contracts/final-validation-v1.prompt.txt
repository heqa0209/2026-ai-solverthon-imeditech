Independently validate whether each supplied semantic or OCR-dependent judgment is fully supported by the immutable company profile, optional latest_answers, condition evidence, prior status, and prior explanation. A latest answer is newer condition-scoped data, not a verdict, and must be evaluated with the condition rather than converted directly to PASS or FAIL. Check the subject, operator, unit, profile field, answer, and verbatim evidence together. Return ACCEPT when correct, CORRECT only with scoped corrections to AI semantic results, and UNRESOLVED when support is ambiguous or required input is absent. A correction for an OCR-backed deterministic condition may only set UNKNOWN; deterministic Python comparisons cannot be changed.
